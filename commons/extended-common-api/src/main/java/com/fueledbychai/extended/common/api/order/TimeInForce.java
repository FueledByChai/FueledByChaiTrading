package com.fueledbychai.extended.common.api.order;

/** Extended time-in-force values. {@code GTT} = good-till-time, {@code IOC} = immediate-or-cancel. */
public enum TimeInForce {
    GTT, IOC, FOK;
}
